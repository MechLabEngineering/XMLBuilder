#include <QApplication>
#include "Testbilder.h"

Testbilder::Testbilder(QWidget *parent) : QWidget(parent)
{
    labelInfo = new QLabel("Diese Seite ist noch im Aufbau.",this);
    //button1 = new QPushButton("Button1",this);

}
